export const styles = (theme) => ({
  userTitleStyle: {
    color: 'white',
    fontSize: '1rem',
    alignSelf: 'center',
  },
  large: {
    width: theme.spacing(15),
    height: theme.spacing(15),
  },
});
