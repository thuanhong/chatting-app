export const styles = (theme) => ({
  cardStyle: {
    backgroundColor: '#2a2a2a',
    position: 'fixed',
    width: 'inherit',
    top: 0,
    zIndex: 99999,
  },
  listMarginTop: {
    marginTop: theme.spacing(25),
  },
});
